# Netlify (and some static hosts): proxy API so the browser can call /sdci-api/* same-origin
# (avoids CORS on GET /health when the backend only whitelists /api/* for browsers).
/sdci-api/*  https://fastview-sdci.onrender.com/:splat  200
